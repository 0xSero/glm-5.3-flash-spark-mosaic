--- a/models/glm5next/nvidia/mtp.py
+++ b/models/glm5next/nvidia/mtp.py
@@ -333,6 +333,28 @@
             # prefix to match.
             if name.startswith("model.language_model."):
                 name = name.replace("model.language_model.", "model.", 1)
+            # Native BF16 companion views retain the target embedding/head.
+            # Load these real tensors before the default loader's strict
+            # completeness check; the proposer subsequently shares target
+            # parameters and releases these temporary draft copies.
+            shared_names = []
+            if name == "model.embed_tokens.weight":
+                shared_names = ["model.embed_tokens.weight"]
+            elif name == "lm_head.weight":
+                shared_names = [
+                    f"model.layers.{idx}.shared_head.head.weight"
+                    for idx in range(
+                        self.model.mtp_start_layer_idx,
+                        self.model.mtp_start_layer_idx + self.model.num_mtp_layers,
+                    )
+                ]
+            if shared_names:
+                for destination in shared_names:
+                    param = params_dict[destination]
+                    loader = getattr(param, "weight_loader", default_weight_loader)
+                    loader(param, loaded_weight)
+                    loaded_params.add(destination)
+                continue
             spec_layer = get_spec_layer_idx_from_weight_name(self.config, name)
             if spec_layer is None:
                 continue
--- a/v1/spec_decode/llm_base_proposer.py
+++ b/v1/spec_decode/llm_base_proposer.py
@@ -1318,6 +1318,19 @@
                 ),
             )
 
+        # GLM MTP constructors read both model_config and quant_config from
+        # VllmConfig. Keep them bound to the draft rather than the EXL3 target.
+        # This is the same contract used by the native Step3.5 MTP proposer.
+        if "Glm5NextMTPModel" in (
+            self.draft_model_config.hf_config.architectures or []
+        ):
+            from vllm.model_executor.models.utils import get_draft_quant_config
+
+            base = replace(
+                base,
+                model_config=self.draft_model_config,
+                quant_config=get_draft_quant_config(base),
+            )
         return base
 
     def _get_model(self) -> nn.Module:
@@ -1376,6 +1389,7 @@
                 "Cohere2VisionForConditionalGeneration",
                 "Exaone4_5_ForConditionalGeneration",
                 "GlmOcrForConditionalGeneration",
+                "Glm5NextForConditionalGeneration",
                 "HunYuanVLForConditionalGeneration",
                 "InternS2PreviewForConditionalGeneration",
                 "MiMoV2OmniForCausalLM",
--- a/v1/worker/gpu/spec_decode/eagle/utils.py
+++ b/v1/worker/gpu/spec_decode/eagle/utils.py
@@ -39,6 +39,16 @@
     speculative_config = vllm_config.speculative_config
     assert speculative_config is not None
     draft_model_config = speculative_config.draft_model_config
+    # GLM MTP constructors read model/quantization from VllmConfig itself.
+    # An explicit native draft must not inherit target EXL3 or REAP geometry.
+    if "Glm5NextMTPModel" in (draft_model_config.hf_config.architectures or []):
+        from vllm.model_executor.models.utils import get_draft_quant_config
+
+        vllm_config = replace(
+            vllm_config,
+            model_config=draft_model_config,
+            quant_config=get_draft_quant_config(vllm_config),
+        )
     if speculative_config.kv_cache_dtype is not None:
         vllm_config = replace(
             vllm_config,
